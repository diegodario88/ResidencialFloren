export default class Pharmacy {
  name: string;
  phone: string;
  adress: string;

  constructor(name: string, phone: string, adress: string) {
    this.name = name;
    this.phone = phone;
    this.adress = adress;
  }
}
